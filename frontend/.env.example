# Base URL of the FastAPI backend (Backend/main.py).
# No trailing slash.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Cookie-based auth (access_token httpOnly cookie is set by /api/auth/callback/google).
# This flag just controls whether the client sends credentials with requests.
NEXT_PUBLIC_USE_COOKIE_AUTH=true
