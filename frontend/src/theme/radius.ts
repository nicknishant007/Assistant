/**
 * Radius system. The brand look uses generous rounding on blocks
 * (cards, inputs, avatars) but stays sharp on structural chrome
 * (dividers, the sidebar rail) — that contrast is intentional.
 */
export const radius = {
  none: "0px",
  sm: "8px",
  md: "14px",
  lg: "20px",
  xl: "28px",
  pill: "999px"
} as const;
